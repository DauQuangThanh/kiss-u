# Regression-test index

**Last updated:** {date}

## Coverage

| Bug id | Status | Level | Test file | Notes |
|---|---|---|---|---|
| BUG-NN | Closed | integration | `tests/regression/bug_NN.test.ts` | repro from bug file |

## Rules

- Every bug marked Closed in `change-register.md` must appear above
  with a linked test.
- If a bug can't be reasonably regression-tested (timing-dependent,
  infra-only), the row's Notes column must say so explicitly —
  never blank.
