# Codebase Scan — KISS

> Generated by `technical-analyst` (auto mode) on 2026-04-26.
> Every claim cites `file:line`. Inferences not directly supported by code are
> marked **(unverifiable — confirm)** and logged in `analysis-debts.md`.

## 1. Project identity

- **Name**: `kiss` — `pyproject.toml:2`
- **Version**: `0.3.0` — `pyproject.toml:3`
- **Description**: "KISS — a tool to bootstrap your projects for Spec-Driven
  Development (SDD) with reusable AI components." — `pyproject.toml:4`
- **Python**: `>=3.11` — `pyproject.toml:5`
- **Console-script entry point**: `kiss = "kiss_cli:main"` — `pyproject.toml:23`
- **Module entry point**: `python -m kiss_cli` → `kiss_cli/__main__.py:3` calls
  `from . import main` — `src/kiss_cli/__main__.py:1-7`
- **Repo URL**: `https://github.com/DauQuangThanh/kiss-u` —
  `pyproject.toml:19-20`

## 2. Languages and lines of code

Counts exclude `.venv/`, `.git/`, `build/` (build-time staging,
`.gitignore:6-11`), `__pycache__/`, `.pytest_cache/`, `.ruff_cache/`,
`.claude/` (locally installed copies), and `.kiss/`.

| Language       | Files | Lines | Notes |
|----------------|-------|-------|-------|
| Python         | 105   | 37,528 | `find … -name '*.py'` enumeration |
|   ↳ in `src/kiss_cli/` | 55 | 17,292 | Production code |
|   ↳ in `tests/`        | 48 | 20,010 | Test suite |
|   ↳ in `scripts/`      | 2  | hatch hook + checksum generator |
| Markdown       | 244+  | n/a   | Skill prompts, presets, docs |
| YAML           | 36    | n/a   | Manifests + GitHub Actions |
| Bash (`*.sh`)  | 96+   | n/a   | 92 in `agent-skills/`, 4 in `extensions/` |
| PowerShell     | 96+   | n/a   | 92 in `agent-skills/`, 4 in `extensions/` |

Hot files by LOC (top 10):

| LOC | File |
|-----|------|
| 2,493 | `src/kiss_cli/extensions.py` |
| 2,098 | `src/kiss_cli/presets.py` |
| 1,374 | `src/kiss_cli/integrations/base.py` |
| 1,205 | `src/kiss_cli/cli/extension.py` |
| 778   | `src/kiss_cli/workflows/engine.py` |
| 709   | `src/kiss_cli/agents.py` |
| 669   | `src/kiss_cli/cli/preset.py` |
| 639   | `src/kiss_cli/cli/check.py` |
| 637   | `src/kiss_cli/installer.py` |
| 599   | `src/kiss_cli/cli/init.py` |

(Sourced from `wc -l` output collected during scan.)

## 3. Frameworks, runtimes, package managers

Declared runtime dependencies — `pyproject.toml:6-16`:

- `typer>=0.24.2` — CLI framework (Typer/Click)
- `click>=8.3.3` — Click is Typer's underpinning
- `rich` — terminal rendering
- `platformdirs` — XDG / OS-specific paths
- `readchar` — interactive single-key reads (used in
  `src/kiss_cli/ui.py` for the multi-select UI — see `cli/init.py:31`)
- `pyyaml>=6.0` — YAML for `.kiss/context.yml`, workflows, presets,
  extensions
- `packaging>=26.1` — used by `extensions.py:25-26` and `presets.py:27-28`
- `pathspec>=1.1.0` — used by `extensions.py:22`
- `json5>=0.14.0` — JSONC / VS Code settings parsing
  (`src/kiss_cli/installer.py:6,201`)

Optional `[test]` and `[dev]` extras — `pyproject.toml:72-81`:

- `pytest>=7.0`, `pytest-cov>=4.0`
- `skills-ref>=0.1.1` (dev only — agentskills.io compliance validator,
  used in `.github/workflows/validate-skills.yml:46-57`)

Build system — `pyproject.toml:25-28`:

- `hatchling` build backend
- Custom build hook: `scripts/hatch_build_hooks.py` —
  `pyproject.toml:29-30`, defined as `CustomBuildHook` in
  `scripts/hatch_build_hooks.py:104`.

Package manager: `uv.lock` (uv) is the lockfile format —
top-level `uv.lock:1` (`version = 1`). The repo uses `uv` per
`.github/workflows/test.yml:18-19` (`astral-sh/setup-uv@…`).

No JavaScript / Node packages — no `package.json` at the repo root.

## 4. Entry points

### CLI

- **Console script**: `kiss` → `kiss_cli:main` — `pyproject.toml:23`,
  defined in `src/kiss_cli/__init__.py:407-409`.
- **Module form**: `python -m kiss_cli` — `src/kiss_cli/__main__.py:3-7`.
- **Typer app**: created in `src/kiss_cli/cli/__init__.py:12-18`. Sub-apps
  registered there at lines `47-53`:
  - `version` — `cli/version.py:12-13` (`@app.command()`)
  - `check` — `cli/check.py:26-31` (sub-Typer), registered
    `cli/check.py:639`
  - `init` — `cli/init.py:76-77` (`@app.command()`)
  - `integration <list|install|uninstall|switch|upgrade>` —
    `cli/integration.py:89,91,181,261,345,472`
  - `preset <list|add|remove|search|resolve|info|set-priority|enable|disable>`
    plus `preset catalog <list|add|remove>` — `cli/preset.py:85,93–629`
  - `extension <list|add|remove|search|info|update|enable|disable|set-priority>`
    plus `extension catalog <list|add|remove>` — `cli/extension.py:90,98,
    100,143,202,273,315,422,496,582,674,1063,1109,1157`
  - `workflow <run|resume|status|list|add|remove|search|info>` plus
    `workflow catalog <list|add|remove>` — `cli/workflow.py:85,93,95,164,
    199,262,290,391,419,454,526,549,572`

### Build / packaging entry points

- **Hatch build hook**: `scripts/hatch_build_hooks.py:104` —
  `class CustomBuildHook(BuildHookInterface)` with `initialize()` at
  `scripts/hatch_build_hooks.py:107`. Wired in `pyproject.toml:29-30`.
- **Checksum generator**: `scripts/generate-checksums.py:71` (`main()`),
  invoked from the build hook —
  `scripts/hatch_build_hooks.py:129-138`.

### `.devcontainer` post-create

- `/Users/thanhdq.dxhub/00-Github/kiss-u/.devcontainer/post-create.sh`
  (referenced by `.devcontainer/devcontainer.json`).

## 5. Tests and coverage signals

- Test framework: `pytest` — `pyproject.toml:84-92`. Test paths fixed to
  `tests/` (`pyproject.toml:84`). Test class/file/function naming
  conventions declared at `pyproject.toml:85-87`.
- Test count: 48 Python files / ~20,010 lines (≈54% of total Python LOC).
- Coverage tool: `coverage` v7.13.5 (uv.lock entry — `uv.lock:36`),
  configured at `pyproject.toml:94-101` with `source = ["src"]`,
  `omit = ["*/tests/*", "*/__pycache__/*"]`. No coverage thresholds set.
- CI runs pytest on a 6-cell matrix: `{ubuntu-latest, windows-latest} ×
  {Python 3.11, 3.12, 3.13}` — `.github/workflows/test.yml:30-55`.
- Cross-platform notes: `tests/conftest.py::_has_working_bash()` is
  referenced as the gatekeeper for skipping bash tests on Windows when
  no Git-for-Windows MSYS bash is available —
  `.github/workflows/test.yml:50-53`.
- A separate compliance suite —
  `tests/test_agent_skills_compliance.py` — runs in
  `.github/workflows/validate-skills.yml:59-60`.

Notable test files:

- `tests/test_init_multi.py` — multi-AI install flow.
- `tests/test_offline.py` — verifies no network access during init.
- `tests/test_asset_integrity.py` — sha256 checksum validation.
- `tests/test_workflows.py` — workflow engine tests.
- `tests/test_extensions.py`, `tests/test_presets.py` — manager tests.

## 6. Build, lint, and format tooling

- **Build**: `uv build --wheel --sdist` — `.github/workflows/release.yml:62`.
  Custom build hook stages assets and recomputes checksums before hatchling
  packages — `scripts/hatch_build_hooks.py:104-144`.
- **Linter (Python)**: `ruff` — `.github/workflows/test.yml:26-27`
  (`uvx ruff check src/`). The `.ruff_cache/` directory exists in repo
  (`.ruff_cache/0.15.12/`). No `[tool.ruff]` section is present in
  `pyproject.toml` (defaults apply).
- **Linter (Markdown)**: `markdownlint-cli2` —
  `.github/workflows/lint.yml:17-22`. Config at
  `.markdownlint-cli2.jsonc:1-25`. Ignores `.genreleases/`
  (`.markdownlint-cli2.jsonc:23-25`).
- **Security scan**: GitHub CodeQL — `.github/workflows/codeql.yml:1-32`,
  matrix on `actions` and `python` (`codeql.yml:18-19`).
- **Skills validator**: `skills-ref` (dev dep) —
  `.github/workflows/validate-skills.yml:46-57`.
- **Dependabot**: weekly updates for both pip and github-actions —
  `.github/dependabot.yml:1-11`.

## 7. Asset trees authoritative at the repo root

Per `CLAUDE.md` and confirmed by `scripts/hatch_build_hooks.py:32-48`:

- `agent-skills/` — 49 skill bundles (one per `kiss-<name>/` folder).
  Build hook excludes `_template/` (prefixes `_`) — `hatch_build_hooks.py:60-71`.
- `subagents/` — 14 role-agent prompts (architect, developer,
  technical-analyst, …) — listed in `subagents/`.
- `presets/` — 3 presets (`lean/`, `scaffold/`, `self-test/`), each with a
  `preset.yml` — see `find … preset.yml`.
- `extensions/` — 3 extensions (`git/`, `selftest/`, `template/`), each
  with an `extension.yml`.
- `workflows/` — 1 bundled workflow (`kiss/workflow.yml`) plus a catalog
  `workflows/catalog.json` and architecture/publishing docs.
- `integrations/catalog.json` — bundled integrations catalog
  (only the JSON file is staged; integration code itself ships under
  `src/kiss_cli/integrations/`) — `hatch_build_hooks.py:46-48`.

The build hook copies these into `build/core_pack/` and hatchling
remaps that to `kiss_cli/core_pack/` inside the wheel via
`force-include` — `pyproject.toml:41-42`. `build/` is gitignored
(`.gitignore:6-11`).

## 8. Source-tree summary

```text
src/kiss_cli/
├── __init__.py            # 413 LOC — package init, helpers, exports
├── __main__.py            #   7 LOC — `python -m kiss_cli`
├── _bundled_catalogs.py   #  63 LOC — offline catalog loader
├── _integrity.py          #  98 LOC — SHA-256 verification
├── agents.py              # 709 LOC — CommandRegistrar
├── config.py              #  33 LOC — `.kiss/init-options.json`
├── context.py             # 187 LOC — `.kiss/context.yml`
├── extensions.py          # 2,493 LOC — extension manager
├── installer.py           # 637 LOC — install primitives
├── presets.py             # 2,098 LOC — preset manager
├── skill_assets.py        # 126 LOC — per-skill asset bundling
├── tracker.py             #  89 LOC — Rich step tree UI
├── ui.py                  # 252 LOC — banner / multi-select / keys
├── version.py             #  23 LOC — version lookup
├── cli/                   # 8 files — Typer command modules
│   ├── __init__.py        #  53 LOC — main app
│   ├── version.py         #  38 LOC
│   ├── check.py           # 639 LOC
│   ├── init.py            # 599 LOC
│   ├── integration.py     # 582 LOC
│   ├── preset.py          # 669 LOC
│   ├── extension.py       # 1,205 LOC
│   └── workflow.py        # 592 LOC
├── integrations/          # 13 integrations + base/manifest/catalog
│   ├── __init__.py        # registry, _register_builtins
│   ├── base.py            # 1,374 LOC
│   ├── catalog.py         # 511 LOC
│   ├── manifest.py        # 265 LOC
│   ├── claude/, copilot/, gemini/, opencode/, cursor_agent/, agy/,
│   │ auggie/, codex/, kilocode/, kiro_cli/, tabnine/, windsurf/, generic/
└── workflows/
    ├── __init__.py        # STEP_REGISTRY + builtin step registration
    ├── base.py            # StepBase, StepContext, StepResult, enums
    ├── engine.py          # WorkflowEngine, WorkflowDefinition
    ├── catalog.py         # WorkflowRegistry
    ├── expressions.py     # expression evaluator
    └── steps/             # 10 step types: command, shell, prompt, gate,
                          #   if_then, switch, while_loop, do_while,
                          #   fan_out, fan_in
```

## 9. Cross-check with intake docs

- `docs/standards.md` exists with the project's KISS/YAGNI/test-first
  principles — `docs/standards.md:1-30`.
- No `docs/architecture/intake.md` is present (verified by scan). No ADR
  files under `docs/decisions/` (verified). One `ANALYSISDEBT-001` is
  logged so the architect can later seed those.

## 10. Scope and exclusions

- **Scanned**: full repo tree per `.kiss/context.yml` defaults.
- **Excluded**: `.venv/`, `.git/`, `build/`, `dist/`, `__pycache__/`,
  `.pytest_cache/`, `.ruff_cache/`, `.claude/` (locally installed
  copies), and `.kiss/` per CLAUDE.md (build-time / generated).
